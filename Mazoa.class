package mazoa;
import java.util.ArrayList;
import java.util.Iterator;

public class Mazoa
{
  private KartaZerrenda mazoKartak;
  private static Mazoa nireMazoa = null;
  private int i = 0;

  private Mazoa() 
  { 
    this.mazoKartak= new KartaZerrenda(); 
  }

  public static Mazoa getMazoa()
  {
    if (nireMazoa==null) 
    {
      nireMazoa = new Mazoa();
    }
    return nireMazoa;
  }

  private Iterator<Karta> getIteradorea()
  {
    return mazoKartak.iterator();
  }

  public void kenduKarta(Karta pKarta)
  {
    this.mazoKartak.remove(pKarta);
  }

  public Karta kartaEman()
  {
    return mazoKartak.get(0);
    mazoKartak.remove(0);
  }

  public void kartaSortu() {
    
    // Horia
    for (int i = 0; i < 19; i++) {
        Karta h = new Karta();
        h.kolorea = "Horia";
        h.zenbakia = (i % 10 == 0) ? 0 : (i % 10);
    }

    // Urdina
    for (int i = 0; i < 19; i++) {
        Karta u = new Karta();
        u.kolorea = "Urdina";
        u.zenbakia = (i % 10 == 0) ? 0 : (i % 10);
    }

    // Gorria
    for (int i = 0; i < 19; i++) {
        Karta g = new Karta();
        g.kolorea = "Gorria";
        g.zenbakia = (i % 10 == 0) ? 0 : (i % 10);
    }

    // Berdea
    for (int i = 0; i < 19; i++) {
        Karta b = new Karta();
        b.kolorea = "Berdea";
        b.zenbakia = (i % 10 == 0) ? 0 : (i % 10);
    }
}




  /* HACER MÁS TARDE
  private void kartaHartu(Karta pKarta)
  {
    this.mazoKartak.add(pKarta);
  }
  */

}
