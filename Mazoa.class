package mazoa;
import java.util.ArrayList;
import java.util.Iterator;

public class Mazoa
{
  private KartaZerrenda mazoKartak;
  private static Mazoa nireMazoa = null;
  private int i = 0;

  private Mazoa() 
  { 
    this.mazoKartak= new KartaZerrenda(); 
  }

  public static Mazoa getMazoa()
  {
    if (nireMazoa==null) 
    {
      nireMazoa = new Mazoa();
    }
    return nireMazoa;
  }

  private Iterator<Karta> getIteradorea()
  {
    return mazoKartak.iterator();
  }

  public void kenduKarta(Karta pKarta)
  {
    this.mazoKartak.remove(pKarta);
  }

  public Karta kartaEman()
  {
    return mazoKartak.get(0);
    mazoKartak.remove(0);
  }

  public void kartaSortu()
  {
    //Horia
    Karta h0 = new Karta;
    h0.kolorea = "Horia";
    h0.zenbakia = 0; 

    while (i < 10)
    {
       Karta h = new Karta;
       h.kolorea = "Horia";
       h.zenbakia = i;
    }

    //Urdina 
    Karta u0 = new Karta;
    u0.kolorea = "Urdina";
    u0.zenbakia = 0; 

    while (i < 10)
    {
       Karta h = new Karta;
       h.kolorea = "Urdina";
       h.zenbakia = i;
    }

    //Gorria 
    Karta g0 = new Karta;
    g0.kolorea = "Gorria";
    g0.zenbakia = 0; 

    while (i < 10)
    {
       Karta h = new Karta;
       h.kolorea = "Gorria";
       h.zenbakia = i;
    }

    //Berdea 
    Karta b0 = new Karta;
    b0.kolorea = "Berdea";
    b0.zenbakia = 0;

    while (i < 10)
    {
       Karta h = new Karta;
       h.kolorea = "Berdea";
       h.zenbakia = i;
    }  
 }



  /* HACER MÁS TARDE
  private void kartaHartu(Karta pKarta)
  {
    this.mazoKartak.add(pKarta);
  }
  */

}
